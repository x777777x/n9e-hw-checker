all:
  children:
    # 联动服务主机（Go 服务 + systemd）
    linkage:
      hosts:
        linkage01:
          ansible_host: 10.0.0.10

    # 受监控服务器（采集端 kernel-hw-mon）
    monitored:
      hosts:
        server01:
          ansible_host: 10.0.0.11
